# 数学建模项目工作快照

本仓库保存 `E:\codexh\2026-09-23\ai` 项目的代码、分析过程和当前成果。`outputs/` 是对外查看的报告、程序与结果数据；`work/` 包含计算、核验、报告生成脚本及题目文本摘录。

这是 2026-09-23 上传时的快照。原任务仍在另一个窗口运行，后续生成的文件不会自动同步到本仓库。

## 数据依赖

部分脚本读取本机 `E:\数学建模\...` 下的原始赛题附件、地理空间数据和无人机基础数据；这些文件不在原项目目录内，因此不在本仓库中。要在其他电脑重跑，请取得相应原始数据，并修改脚本中的本地路径。

`outputs/d_q1.py` 和 `outputs/d_q2.py` 是整理后的问题一、问题二程序；问题二程序运行时需要与问题一程序放在同一目录。

